export enum StatusEnum {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum UserRoleEnum {
  RECRUITER = "RECRUITER",
  EMPLOYEE = "EMPLOYEE",
  CANDIDATE = "CANDIDATE",
}

export enum WorkLocationTypeEnum {
  ON_SITE = "ON_SITE",
  HYBRID = "HYBRID",
  REMOTE = "REMOTE",
}